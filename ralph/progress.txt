# Ralph Progress Log
Started: Fri Aug 28 12:39:35 PM PDT 2026
---

## Codebase Patterns
- Stack: Next.js 16 (App Router, Turbopack), React 19, TypeScript 7, pnpm. `pnpm typecheck` = `tsc --noEmit`, `pnpm test` = `node --test` (node built-in test runner, no framework).
- `better-sqlite3` is native: it must stay in `serverExternalPackages` in next.config.ts and in `pnpm.onlyBuiltDependencies` in package.json, otherwise the bundler/installer breaks it.
- `output: 'standalone'` — verify a build with `pnpm build && PORT=xxxx node .next/standalone/server.js`.
- Next rewrites tsconfig.json on build (jsx -> react-jsx, adds .next/dev/types include). Don't fight it; commit the rewritten file.
- DB path comes from env `DATABASE_PATH` (default ./data/review.db); `data/` is gitignored.

## 2026-08-28 - US-001
- Scaffolded the Next.js app by hand (no create-next-app): package.json, next.config.ts, tsconfig.json, app/layout.tsx, app/page.tsx placeholder.
- Installed next/react/react-dom/better-sqlite3 + TS types; approved better-sqlite3 build script via pnpm.onlyBuiltDependencies.
- Verified: `pnpm typecheck` clean, `pnpm build` clean, standalone server returns 200 with "Review" at /.
- Files changed: package.json, pnpm-lock.yaml, next.config.ts, tsconfig.json, next-env.d.ts, app/layout.tsx, app/page.tsx, .gitignore
- **Learnings for future iterations:**
  - Hand-scaffolding is cleaner than create-next-app here (no eslint/tailwind noise); add tooling only when a story needs it.
  - better-sqlite3 13 ships prebuilt binaries for node 22, so no compile toolchain issues on this host.
  - Add `*.tsbuildinfo` to .gitignore (incremental: true generates it).
  - No test files exist yet; `node --test` currently passes vacuously. US-002 adds the first real tests.
---

## 2026-08-28 - US-002
- Added `lib/db.ts`: single lazy `getDb()` singleton opening SQLite at `DATABASE_PATH` (default ./data/review.db), mkdir -p on the dir, WAL + foreign_keys pragmas, and idempotent `CREATE TABLE IF NOT EXISTS` schema for projects/tokens/comments + indexes. Exported `Project`/`Token`/`Comment`/`CommentType`/`CommentStatus` types for later API/UI stories.
- Comments carry `notified_at` (nullable) for the US-009 email debounce; tokens carry `last_notified_at` and `revoked_at`.
- Added `lib/db.test.ts` (node:test): inserts project → token → comment into a tmpdir DB and reads back, asserting defaults (status 'open', parent_id/notified_at null) and WAL mode.
- Enabled `allowImportingTsExtensions` in tsconfig.json (see learnings).
- Files changed: lib/db.ts, lib/db.test.ts, tsconfig.json, ralph/prd.json, ralph/progress.txt
- Verified: `pnpm test` 1/1 pass, `pnpm typecheck` clean, `pnpm build` clean.
- **Learnings for future iterations:**
  - Node 22 strips TS types natively but does NOT rewrite import specifiers, so test files must import with the literal `.ts` extension (`./db.ts`). That requires `allowImportingTsExtensions: true` in tsconfig (safe under `noEmit`). App/server code should keep using the `@/lib/...` alias — Next resolves that fine.
  - To point a test at its own DB, set `process.env.DATABASE_PATH` then `await import('./db.ts')` — a top-level static import would run the singleton before the env assignment.
  - `node --test` warns MODULE_TYPELESS_PACKAGE_JSON on .ts test files; harmless, don't add `"type": "module"` just to silence it.
---
